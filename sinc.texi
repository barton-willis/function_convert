@ifnotinfo
@settitle Function sinc
@top Function sinc
@end ifnotinfo

@deffn Function sinc @var{x}
The function @code{sinc} is defined by @code{sinc(x) = sin(x)/x}
for @code{@var{x} ≠ 0}, and by @code{sinc(0) = 1}, which makes
@code{sinc} continuous at zero. This function is sometimes known
as the @emph{unnormalized} @code{sinc} function.


When @code{%piargs} is true (the default), @code{sinc(@var{x})}
simplifies to an exact value when @var{x} is an explicit integer multiple of
@code{pi}, @code{pi/4}, or @code{pi/6}.

@example
(%i1) sinc(%pi);
(%o1) 0

(%i2) %piargs : false$

(%i3) sinc(pi);
(%o3) sinc(%pi)
@end example
@end deffn


